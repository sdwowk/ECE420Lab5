Task 1 steps:
	val rdd = sc.textFile("data_input")
	val inLinks = rdd.map(line => line.replace("(","").replace(")","").split(",")).map(word =>(word, 1)).reduceByKey(_ + _)
	val maxInLinks =  inLinks.max()(new Ordering[Tuple2[String, Int]](){ override def compare(x: (String, Int), y: (String, Int)): Int = Ordering[Int].compare(x._2, y._2)})

	//RETURNS maxInLinks: (String, Int) = (7,30871)

